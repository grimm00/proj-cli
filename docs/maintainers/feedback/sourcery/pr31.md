# Sourcery Review Analysis
**PR**: #31
**Repository**: grimm00/proj-cli
**Generated**: Tue Jun  9 10:49:36 CDT 2026

---

## Summary

Total Individual Comments: 1 + Overall Comments

## Individual Comments

### Comment #1

**Type**: issue (bug_risk)

**Description**: `roots = skill_roots or DEFAULT_SKILL_ROOTS` treats an explicitly empty `skill_roots` as falsy and silently replaces it with `DEFAULT_SKILL_ROOTS`, so callers can’t deliberately pass “no roots”.

<details>
<summary>Details</summary>

<b>Code Context</b>

<pre><code>
+    return [name for name in skills if isinstance(name, str) and name.strip()]
+
+
+def is_skill_installed(
+    skill_name: str,
+    skill_roots: Optional[tuple[Path, ...]] = None,
+) -&gt; bool:
+    &quot;&quot;&quot;Return True when a skill directory exists under any global skills root.&quot;&quot;&quot;
+    roots = skill_roots or DEFAULT_SKILL_ROOTS
+    for root in roots:
+        if (root / skill_name).is_dir():
</code></pre>

<b>Issue</b>

**issue (bug_risk):** Respect explicitly provided empty skill_roots instead of falling back to defaults

</details>

---

## Overall Comments

- In `load_expected_skills`, consider catching `yaml.YAMLError` and returning an empty list (or a clear warning) so that malformed `.dev-infra.yml` files don’t unexpectedly break `proj create` despite the intent for this validation to be warn-only.
- `warn_missing_expected_skills` currently calls `load_expected_skills` directly and then re-calls it via `find_missing_skills`; you could pass the already-loaded list into `find_missing_skills` (or refactor) to avoid reading and parsing `.dev-infra.yml` twice per invocation.

## Priority Matrix Assessment

| Comment | Priority | Impact | Effort | Action | Notes |
|---------|----------|--------|--------|--------|-------|
| #1 (empty `skill_roots` falsy) | 🟡 MEDIUM | 🟡 MEDIUM | 🟢 LOW | **Fix in-line** | Real bug risk: tests could pass an empty tuple and hit defaults silently. Simple `is None` check, 3 lines. |
| Overall-1 (catch `yaml.YAMLError`) | 🟠 HIGH | 🟠 HIGH | 🟢 LOW | **Fix in-line** | Violates the warn-not-error contract if `.dev-infra.yml` has malformed YAML. One try/except, ~3 lines. |
| Overall-2 (double-read of `.dev-infra.yml`) | 🟢 LOW | 🟢 LOW | 🟢 LOW | Defer | Cosmetic. File is tiny YAML. No user impact. |

### Priority Levels
- 🔴 **CRITICAL**: Security, stability, or core functionality issues
- 🟠 **HIGH**: Bug risks or significant maintainability issues
- 🟡 **MEDIUM**: Code quality and maintainability improvements
- 🟢 **LOW**: Nice-to-have improvements

### Impact Levels
- 🔴 **CRITICAL**: Affects core functionality
- 🟠 **HIGH**: User-facing or significant changes
- 🟡 **MEDIUM**: Developer experience improvements
- 🟢 **LOW**: Minor improvements

### Effort Levels
- 🟢 **LOW**: Simple, quick changes
- 🟡 **MEDIUM**: Moderate complexity
- 🟠 **HIGH**: Complex refactoring
- 🔴 **VERY_HIGH**: Major rewrites

### Action Summary

- **Fix in-line (2):** Comment #1 (empty `skill_roots`) + Overall-1 (`yaml.YAMLError`)
- **Defer (1):** Overall-2 (double-read, cosmetic)


