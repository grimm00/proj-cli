# Sourcery Review Analysis
**PR**: #17
**Repository**: grimm00/proj-cli
**Generated**: Wed Jan  7 08:45:19 CST 2026

---

## Summary

Total Individual Comments: 2 + Overall Comments

## Individual Comments

### Comment #1

**Location**: `tests/test_cli_integration.py:204-211`

**Type**: suggestion (testing)

**Description**: To fully exercise the CLI init flow and `Config.load()`, please also assert that `config.registry.path` and `config.default_project_dir` match the corresponding values in `saved`, similar to the assertions in `test_config_load_nested_from_yaml`. That would complete the load-time validation of the new fields.

<details>
<summary>Details</summary>

<b>Code Context</b>

<pre><code>

+    # Validate loaded Config values
+    from proj.config import Config
+    config = Config.load()
+    assert config.api_enabled is True
+    assert config.templates.default == &#x27;standard-project&#x27;
+    # templates.source is set only if the default templates directory exists
+    # Check that loaded config matches what was saved
+    if &#x27;source&#x27; in saved.get(&#x27;templates&#x27;, {}):
+        assert config.templates.source is not None
+        assert str(config.templates.source) == saved[&#x27;templates&#x27;][&#x27;source&#x27;]
+    else:
+        assert config.templates.source is None
</code></pre>

<b>Issue</b>

**suggestion (testing):** Extend CLI init test to validate `registry.path` and `default_project_dir` on the loaded `Config` as well

</details>

---

### Comment #2

**Location**: `tests/test_config.py:188-191`

**Type**: suggestion (testing)

**Description**: You could strengthen `test_config_save_includes_new_fields` by also asserting that `saved['templates']['default']`, `saved['registry']['path']`, and `saved['default_project_dir']` equal the expected values from the test setup (assuming they’re deterministic). This would catch cases where the structure and types are correct but incorrect values are written.

<details>
<summary>Details</summary>

<b>Code Context</b>

<pre><code>
+    assert isinstance(saved[&#x27;registry&#x27;], dict)
+    assert isinstance(saved[&#x27;default_project_dir&#x27;], str)
+
+    # Check nested structure
+    assert &#x27;source&#x27; in saved[&#x27;templates&#x27;]
+    assert &#x27;default&#x27; in saved[&#x27;templates&#x27;]
+    assert &#x27;path&#x27; in saved[&#x27;registry&#x27;]
+
</code></pre>

<b>Issue</b>

**suggestion (testing):** Consider asserting the values (not only presence/types) of the newly added nested fields in the save() test

</details>

---

## Overall Comments

- In `test_init_creates_config_with_new_fields`, consider moving `from proj.config import Config` to the module level so imports are consistent with the rest of the tests and avoid re-importing on each test run.
- The new assertion `assert 'source' in saved['templates']` in `test_config_save_includes_new_fields` assumes `templates.source` is always present, whereas the CLI test explicitly treats it as optional; it may be safer to mirror that optional behavior in the save test (e.g., assert conditional presence or explicitly set up the directory so the expectation is deterministic).

## Priority Matrix Assessment

| Comment | Priority | Impact | Effort | Status | Notes |
|---------|----------|--------|--------|--------|-------|
| #1 | 🟢 LOW | 🟢 LOW | 🟢 LOW | Deferred | Extend CLI init test to validate registry.path and default_project_dir - additional test coverage improvement |
| #2 | 🟢 LOW | 🟢 LOW | 🟢 LOW | Deferred | Assert actual values (not just presence) in save test - additional test validation |
| Overall-#1 | 🟢 LOW | 🟢 LOW | 🟢 LOW | Deferred | Move import to module level for consistency |
| Overall-#2 | 🟢 LOW | 🟢 LOW | 🟢 LOW | Deferred | templates.source optional handling in save test |

### Priority Levels
- 🔴 **CRITICAL**: Security, stability, or core functionality issues
- 🟠 **HIGH**: Bug risks or significant maintainability issues
- 🟡 **MEDIUM**: Code quality and maintainability improvements
- 🟢 **LOW**: Nice-to-have improvements

### Impact Levels
- 🔴 **CRITICAL**: Affects core functionality
- 🟠 **HIGH**: User-facing or significant changes
- 🟡 **MEDIUM**: Developer experience improvements
- 🟢 **LOW**: Minor improvements

### Effort Levels
- 🟢 **LOW**: Simple, quick changes
- 🟡 **MEDIUM**: Moderate complexity
- 🟠 **HIGH**: Complex refactoring
- 🔴 **VERY_HIGH**: Major rewrites


