# Sourcery Review Analysis
**PR**: #19
**Repository**: grimm00/proj-cli
**Generated**: Wed Jan  7 09:27:15 CST 2026

---

## Summary

Total Individual Comments: 2 + Overall Comments

## Individual Comments

### Comment #1

**Location**: `tests/test_create_integration.py:91-100`

**Type**: suggestion (testing)

**Description**: Right now the test shows the API path is used, but not that the local/template path is avoided. To better lock in the fallback behavior, patch the local creation helper(s) (e.g. `create_from_template`, `add_project`) and assert they are not called, alongside the existing `create_project` assertion:

<details>
<summary>Details</summary>

<b>Code Context</b>

<pre><code>
     assert &quot;123&quot; in result.output


+@patch(&#x27;proj.commands.projects.get_client&#x27;)
+@patch(&#x27;proj.commands.projects.Config.load&#x27;)
+def test_create_name_only_falls_back_to_api(
+    mock_config_load, mock_get_client, tmp_path
+):
+    &quot;&quot;&quot;Providing only a name falls back to API (backward compatible).&quot;&quot;&quot;
+    mock_config = MagicMock()
+    mock_config.api_enabled = True
+    mock_config_load.return_value = mock_config
+
+    mock_client = MagicMock()
+    mock_client.create_project.return_value = {
+        &quot;id&quot;: 456,
+        &quot;name&quot;: &quot;My Application&quot;,
+        &quot;status&quot;: &quot;active&quot;,
+    }
+    mock_get_client.return_value = mock_client
+
+    result = runner.invoke(app, [&quot;create&quot;, &quot;My Application&quot;])
+
+    assert result.exit_code == 0
+    mock_client.create_project.assert_called_once()
+    assert &quot;Created project&quot; in result.output
+
+
</code></pre>

<b>Issue</b>

**suggestion (testing):** Also assert that local/template creation helpers are not used to prove API fallback

</details>

---

### Comment #2

**Location**: `tests/test_commands_projects.py:150-159`

**Type**: suggestion (testing)

**Description**: You’ve covered `template + api_only` and `template + local_only`, and there’s already a test for `api_only` and `local_only` both `True` when `template=None`. To fully cover the conflict behavior, please add a case where both flags are `True` and a `template` is provided, e.g.:

<details>
<summary>Details</summary>

<b>Code Context</b>

<pre><code>
     assert mode == &quot;template&quot;


+def test_detect_mode_template_with_api_only_flag():
+    &quot;&quot;&quot;Explicit api_only flag takes precedence over template.&quot;&quot;&quot;
+    mode = detect_create_mode(
+        template=&quot;standard-project&quot;,
+        api_only=True,
+        local_only=False,
+    )
+    assert mode == &quot;api-only&quot;
+
+
+def test_detect_mode_template_with_local_only_flag():
+    &quot;&quot;&quot;Explicit local_only flag takes precedence over template.&quot;&quot;&quot;
+    mode = detect_create_mode(
+        template=&quot;standard-project&quot;,
+        api_only=False,
+        local_only=True,
+    )
+    assert mode == &quot;local-only&quot;
+
+
</code></pre>

<b>Issue</b>

**suggestion (testing):** Add a test for conflicting flags when a template is also provided

</details>

---

## Overall Comments

- Since the new tests codify the precedence of explicit mode flags over the template, consider updating the `detect_create_mode` docstring (and any related CLI help text, if present) to explicitly describe that precedence so callers don’t need to infer it from tests.

## Priority Matrix Assessment

| Comment | Priority | Impact | Effort | Status | Notes |
|---------|----------|--------|--------|--------|-------|
| #1 | 🟢 LOW | 🟡 MEDIUM | 🟢 LOW | Deferred | Add assert_not_called for local helpers in API fallback test |
| #2 | 🟢 LOW | 🟡 MEDIUM | 🟢 LOW | Deferred | Add conflict test with template + both flags |
| Overall #1 | 🟢 LOW | 🟢 LOW | 🟢 LOW | Deferred | Update docstring to describe precedence |

**Summary:** No CRITICAL or HIGH priority issues. All 3 suggestions are LOW priority test/doc improvements that can be deferred.

### Priority Levels
- 🔴 **CRITICAL**: Security, stability, or core functionality issues
- 🟠 **HIGH**: Bug risks or significant maintainability issues
- 🟡 **MEDIUM**: Code quality and maintainability improvements
- 🟢 **LOW**: Nice-to-have improvements

### Impact Levels
- 🔴 **CRITICAL**: Affects core functionality
- 🟠 **HIGH**: User-facing or significant changes
- 🟡 **MEDIUM**: Developer experience improvements
- 🟢 **LOW**: Minor improvements

### Effort Levels
- 🟢 **LOW**: Simple, quick changes
- 🟡 **MEDIUM**: Moderate complexity
- 🟠 **HIGH**: Complex refactoring
- 🔴 **VERY_HIGH**: Major rewrites


