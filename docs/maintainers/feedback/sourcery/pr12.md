# Sourcery Review Analysis
**PR**: #12
**Repository**: grimm00/proj-cli
**Generated**: Tue Jan  6 10:08:18 CST 2026

---

## Summary

Total Individual Comments: 5 + Overall Comments

## Individual Comments

### Comment #1

**Location**: `src/proj/commands/projects.py:64-73`

**Type**: issue

**Description**: `prompt_for_create_options` passes `available = list_templates(templates_source)` directly to `Prompt.ask` as `choices`. If `available` is empty, this will prompt the user to select from no options, likely causing an error or confusing UX. Consider explicitly handling the empty case by either failing with a clear message or skipping the template question and using a non-template flow.

<details>
<summary>Details</summary>

<b>Code Context</b>

<pre><code>
+        return False
+
+
+def prompt_for_create_options(config: Config) -&gt; dict:
+    &quot;&quot;&quot;Prompt user for create options interactively.
+
+    Args:
+        config: proj-cli configuration.
+
+    Returns:
+        Dict with user choices: name, template, target_dir, description.
+
+    Raises:
+        KeyboardInterrupt: If user cancels (Ctrl+C).
+    &quot;&quot;&quot;
+    name = Prompt.ask(&quot;Project name&quot;)
+
+    # List available templates
+    templates_source = get_templates_source(config)
+    available = list_templates(templates_source)
+    default_template = (
+        config.templates.default if hasattr(config, &#x27;templates&#x27;) and
+        hasattr(config.templates, &#x27;default&#x27;) else None
+    )
+    template = Prompt.ask(
+        &quot;Template type&quot;,
+        choices=available,
</code></pre>

<b>Issue</b>

**issue:** Interactive prompting for template does not handle the case where no templates are available.

</details>

---

### Comment #2

**Location**: `src/proj/commands/projects.py:383-392`

**Type**: issue (bug_risk)

**Description**: Because the dry-run branch returns before `detect_create_mode` runs, it skips the `api_only && local_only` conflict check and the `local-only` + `--template` requirement, so mode-specific validation is not mirrored. For instance, `--dry-run --api-only --local-only` currently succeeds but would fail without `--dry-run`. Consider either calling `detect_create_mode` in dry-run, or at least duplicating the conflict checks there so dry-run reliably indicates whether a real run would be valid.

<details>
<summary>Details</summary>

<b>Code Context</b>

<pre><code>
+        # Handle dry-run mode (preview without side effects)
</code></pre>

<b>Issue</b>

**issue (bug_risk):** Dry-run mode bypasses conflict validation and mode-specific checks, leading to inconsistent behavior vs real execution.

</details>

---

### Comment #3

**Location**: `tests/test_commands_projects.py:130-137`

**Type**: suggestion (testing)

**Description**: In particular, consider tests for `(template="standard", api_only=True, local_only=False)` and `(template="standard", api_only=False, local_only=True)` to assert that explicit mode flags take precedence over the template. This will both document the intended behavior and protect it against future refactors.

<details>
<summary>Details</summary>

<b>Code Context</b>

<pre><code>
+    assert mode == &quot;local-only&quot;
+
+
+def test_detect_mode_template():
+    &quot;&quot;&quot;Test template mode detection.&quot;&quot;&quot;
+    config = MagicMock()
+    mode = detect_create_mode(
+        config=config,
+        template=&quot;standard-project&quot;,
+        api_only=False,
+        local_only=False,
+    )
+    assert mode == &quot;template&quot;
+
+
</code></pre>

<b>Issue</b>

**suggestion (testing):** Consider adding tests for `detect_create_mode` when both a template and an explicit mode flag are provided, to document precedence.

<b>Suggestion</b>

<pre><code>
    config = MagicMock()
    mode = detect_create_mode(
        config=config,
        template=None,
        api_only=False,
        local_only=True,
    )
    assert mode == &quot;local-only&quot;


def test_detect_mode_template_with_api_only_flag():
    &quot;&quot;&quot;Explicit api_only flag takes precedence over template.&quot;&quot;&quot;
    config = MagicMock()
    mode = detect_create_mode(
        config=config,
        template=&quot;standard-project&quot;,
        api_only=True,
        local_only=False,
    )
    assert mode == &quot;api-only&quot;


def test_detect_mode_template_with_local_only_flag():
    &quot;&quot;&quot;Explicit local_only flag takes precedence over template.&quot;&quot;&quot;
    config = MagicMock()
    mode = detect_create_mode(
        config=config,
        template=&quot;standard-project&quot;,
        api_only=False,
        local_only=True,
    )
    assert mode == &quot;local-only&quot;
</code></pre>

</details>

---

### Comment #4

**Location**: `tests/test_create_dry_run.py:24-33`

**Type**: suggestion (testing)

**Description**: Since `dry_run` short-circuits before the interactive prompt branch in `create_project`, please add a test (e.g. `test_create_dry_run_interactive_does_not_prompt`) that patches `proj.commands.projects.Prompt`, runs `proj create --dry-run` without `name`/`--template`, and asserts `Prompt.ask` is never called. This will protect the expectation that dry-run stays non-interactive for CI use.

<details>
<summary>Details</summary>

<b>Code Context</b>

<pre><code>
+@patch(&#x27;proj.commands.projects.create_from_template&#x27;)
+@patch(&#x27;proj.commands.projects.get_templates_source&#x27;)
+@patch(&#x27;proj.commands.projects.Config.load&#x27;)
+def test_create_dry_run_shows_preview(
+    mock_config_load, mock_get_source, mock_create, tmp_path
+):
+    &quot;&quot;&quot;Test dry-run shows preview.&quot;&quot;&quot;
+    mock_config = MagicMock()
+    mock_config.default_project_dir = tmp_path
+    mock_config_load.return_value = mock_config
+
+    templates = tmp_path / &quot;templates&quot;
+    templates.mkdir()
+    standard = templates / &quot;standard-project&quot;
+    standard.mkdir()
+    (standard / &quot;README.md&quot;).write_text(&quot;[Project Name]&quot;)
+    mock_get_source.return_value = templates
+
+    result = runner.invoke(app, [
+        &quot;create&quot;, &quot;my-app&quot;,
+        &quot;--template&quot;, &quot;standard-project&quot;,
+        &quot;--dry-run&quot;,
+    ])
+
+    assert (
+        &quot;would create&quot; in result.output.lower() or
+        &quot;preview&quot; in result.output.lower()
+    )
+    mock_create.assert_not_called()
+
+
</code></pre>

<b>Issue</b>

**suggestion (testing):** Add a dry-run test that ensures interactive prompts are not triggered when no name/template is provided.

</details>

---

### Comment #5

**Location**: `tests/test_create_integration.py:60-90`

**Type**: suggestion (testing)

**Description**: Since `create_project` also falls back to the API when `name` is provided without `--template`/`--api-only`, it would be valuable to add a similar integration test that runs `proj create "My Application"` with no mode flags and asserts `get_client().create_project` is called, so the backward-compatible behavior is covered by tests.

<details>
<summary>Details</summary>

<b>Code Context</b>

<pre><code>
+
+@patch(&#x27;proj.commands.projects.get_client&#x27;)
+@patch(&#x27;proj.commands.projects.Config.load&#x27;)
+def test_create_api_only_backward_compatibility(
+    mock_config_load, mock_get_client, tmp_path
+):
+    &quot;&quot;&quot;Integration test: api-only mode maintains backward compatibility.&quot;&quot;&quot;
+    mock_config = MagicMock()
+    mock_config.api_enabled = True
+    mock_config_load.return_value = mock_config
+
+    mock_client = MagicMock()
+    mock_client.create_project.return_value = {
+        &quot;id&quot;: 123,
+        &quot;name&quot;: &quot;My Application&quot;,
+        &quot;status&quot;: &quot;active&quot;,
+    }
+    mock_get_client.return_value = mock_client
+
+    result = runner.invoke(app, [
+        &quot;create&quot;, &quot;My Application&quot;,
+        &quot;--api-only&quot;,
+    ])
+
+    assert result.exit_code == 0
+    # Should have called API
+    mock_client.create_project.assert_called_once()
+    # Should have shown success message
+    assert &quot;Created project&quot; in result.output
+    assert &quot;123&quot; in result.output
+
+
</code></pre>

<b>Issue</b>

**suggestion (testing):** Consider an additional integration test for the implicit API-only fallback when a name is provided without flags.

<b>Suggestion</b>

<pre><code>
@patch(&#x27;proj.commands.projects.get_client&#x27;)
@patch(&#x27;proj.commands.projects.Config.load&#x27;)
def test_create_api_only_backward_compatibility(
    mock_config_load, mock_get_client, tmp_path
):
    &quot;&quot;&quot;Integration test: api-only mode maintains backward compatibility.&quot;&quot;&quot;
    mock_config = MagicMock()
    mock_config.api_enabled = True
    mock_config_load.return_value = mock_config

    mock_client = MagicMock()
    mock_client.create_project.return_value = {
        &quot;id&quot;: 123,
        &quot;name&quot;: &quot;My Application&quot;,
        &quot;status&quot;: &quot;active&quot;,
    }
    mock_get_client.return_value = mock_client

    result = runner.invoke(app, [
        &quot;create&quot;, &quot;My Application&quot;,
        &quot;--api-only&quot;,
    ])

    assert result.exit_code == 0
    # Should have called API
    mock_client.create_project.assert_called_once()
    # Should have shown success message
    assert &quot;Created project&quot; in result.output
    assert &quot;123&quot; in result.output


@patch(&#x27;proj.commands.projects.get_client&#x27;)
@patch(&#x27;proj.commands.projects.Config.load&#x27;)
def test_create_name_only_falls_back_to_api(
    mock_config_load, mock_get_client, tmp_path
):
    &quot;&quot;&quot;Integration test: providing only a name falls back to API (backward compatible).&quot;&quot;&quot;
    mock_config = MagicMock()
    mock_config.api_enabled = True
    mock_config_load.return_value = mock_config

    mock_client = MagicMock()
    mock_client.create_project.return_value = {
        &quot;id&quot;: 456,
        &quot;name&quot;: &quot;My Application&quot;,
        &quot;status&quot;: &quot;active&quot;,
    }
    mock_get_client.return_value = mock_client

    result = runner.invoke(app, [
        &quot;create&quot;, &quot;My Application&quot;,
    ])

    assert result.exit_code == 0
    # Should have called API even without explicit --api-only
    mock_client.create_project.assert_called_once()
    # Should have shown success message with project details
    assert &quot;Created project&quot; in result.output
    assert &quot;456&quot; in result.output
</code></pre>

</details>

---

## Overall Comments

- The `create_project` function has become quite large and branchy (dry-run, interactive, template, local-only, API-only, fallback), and would be easier to maintain if the major flows (e.g. dry-run preview, template/local-only creation, API-only creation, interactive prompting) were split into smaller helpers that encapsulate each mode’s logic.
- `detect_create_mode` takes a `config` argument but does not use it, which is misleading given the name and tests that set `config.api_enabled`; either remove the unused parameter or incorporate config-driven behavior (e.g. defaulting based on `api_enabled`) so the signature reflects actual usage.
- In `prompt_for_create_options`, `available = list_templates(templates_source)` can be empty, but `Prompt.ask` is still called with `choices=available` and a default derived from it; consider handling the no-templates case explicitly with a clear error message before prompting to avoid confusing runtime errors.

## Priority Matrix Assessment

| Comment | Priority | Impact | Effort | Action |
|---------|----------|--------|--------|--------|
| #1 | 🟠 HIGH | 🟡 MEDIUM | 🟢 LOW | ✅ Fixed - PR #[pending] (pr12-batch-high-low-01) |
| #2 | 🟡 MEDIUM | 🟢 LOW | 🟢 LOW | Defer - dry-run edge case |
| #3 | 🟢 LOW | 🟡 MEDIUM | 🟢 LOW | Defer - add precedence tests |
| #4 | 🟢 LOW | 🟡 MEDIUM | 🟢 LOW | Defer - add dry-run prompt test |
| #5 | 🟢 LOW | 🟡 MEDIUM | 🟢 LOW | Defer - add implicit API test |

**Summary:** No CRITICAL issues. 1 HIGH (empty templates handling), 1 MEDIUM, 3 LOW. All deferred to fix batch.

**Overall Comments Assessment:**
- Large `create_project` function: 🟢 LOW priority, 🟠 HIGH effort - Refactor later
- Unused `config` param in `detect_create_mode`: 🟢 LOW priority, 🟢 LOW effort - Quick cleanup
- Empty templates (same as #1): Already tracked above

### Priority Levels
- 🔴 **CRITICAL**: Security, stability, or core functionality issues
- 🟠 **HIGH**: Bug risks or significant maintainability issues
- 🟡 **MEDIUM**: Code quality and maintainability improvements
- 🟢 **LOW**: Nice-to-have improvements

### Impact Levels
- 🔴 **CRITICAL**: Affects core functionality
- 🟠 **HIGH**: User-facing or significant changes
- 🟡 **MEDIUM**: Developer experience improvements
- 🟢 **LOW**: Minor improvements

### Effort Levels
- 🟢 **LOW**: Simple, quick changes
- 🟡 **MEDIUM**: Moderate complexity
- 🟠 **HIGH**: Complex refactoring
- 🔴 **VERY_HIGH**: Major rewrites


